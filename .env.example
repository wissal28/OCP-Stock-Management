# Copiez ce fichier en ".env" (déjà ignoré par git) et renseignez votre clé pour activer le chatbot
# IA (module "Assistant IA" → mode "Chatbot IA"). Sans cette variable, seul l'Assistant local
# (réponses instantanées basées sur les données de l'app, sans IA) reste disponible.
ANTHROPIC_API_KEY=
